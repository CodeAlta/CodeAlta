# Role Authoring

Role files are markdown files named `roles/<name>.role.md`. Keep role text focused on reusable behavior for an acting CodeAlta session.

Optional frontmatter fields are `description`, `version`, and `max_tokens`.